services:
  - type: web
    name: ml-trading-agent
    env: docker
    plan: free
    autoDeploy: true
    branch: main
    healthCheckPath: /
    buildCommand: ""
    startCommand: "uvicorn api.app:app --host 0.0.0.0 --port 5000"
    envVars:
      - key: BROKER_API_KEY
        sync: false
      - key: OPUNSEAPP_API_KEY
        sync: false
