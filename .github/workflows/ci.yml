name: CI (ML Trading Agent)

on:
<<<<<<< fix/ci-actions
  push:
push:
branches: [ main ]
    branches: [main]
branches: [main]
  pull_request:
pull_request:
branches: [ main ]
    branches: [main]
branches: [main]
branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
build:
runs-on: ubuntu-latest

steps:
=======
push:
branches: [ main ]
branches: [main]
@@ -31,11 +42,28 @@ python-version: '3.11'
python-version: '3.11'
# Install dependencies (pip requirements)
python-version: '3.10'
>>>>>>> main
- uses: actions/checkout@v4
# Check out the code from the repo and set up Python
- uses: actions/checkout@v3
# Set up Python with version 3.11
@@ -29,13 +43,16 @@ steps:
@@ -21,8 +28,10 @@ steps:
- uses: actions/checkout@3
- name: Set up Python
uses: actions/setup-python@v4
uses: actions/setup-python@4
with:
with:
with:
python-version: '3.11'
# Install dependencies (pip requirements)
python-version: '3.11'
<<<<<<< fix/ci-actions

# Install dependencies (pip requirements)
python-version: '3.11'
# Install dependencies (pip requirements)
@@ -74,8 +91,9 @@ run: |
run: ruff check .
# Type check with mypy
- name: Type check with mypy
    - name: Install dependencies
      run: |
- name: Install dependencies
run: |
        python -m pip install --upgrade pip
      python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements.txt
@@ -86,13 +104,13 @@ run: |
     # Type check with mypy
     - name: Type check with mypy
     pip install -r requirements.txt
        pip install ruff mypy pytest
pip install ruff mypy pytest
pip install ruff mypy pytest

- name: Ruff
run: ruff .
    - name: Lint with ruff
      run: ruff .
- name: Lint with ruff
run: ruff .
# Lint with ruff
@@ -62,24 +75,26 @@ run: |
run: ruff .
@@ -123,8 +141,8 @@ run: mypy .
# Test with pytest
- name: Test with pytest
run: pytest
    - name: Type check with mypy
      run: mypy .
- name: Type check with mypy
run: mypy .
- name: Type check with mypy
run: mypy .

@@ -134,7 +152,14 @@ run: pytest
run: pytest
- name: Test with pytest
run: pytest
    - name: Test with pytest
      run: pytest
- name: Test with pytest
run: pytest
=======
python-version: '3.10'
@@ -34,18 +43,22 @@ python-version: '3.11'
python-version: '3.10'
@@ -123,3 +151,4 @@ run: pytest
run: pytest
- name: Type check with mypy
run: mypy .
>>>>>>> main
