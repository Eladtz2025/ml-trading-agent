push:
push:
push:
    branches: [ main ]
branches: [ main ]
branches: [ main ]
    branches: [main]
branches: [main]
branches: [main]
pull_request:
pull_request:
pull_request:
    branches: [ main ]
branches: [ main ]
branches: [ main ]
    branches: [main]
branches: [main]
branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
@@ -21,29 +20,36 @@ jobs:
build:
runs-on: ubuntu-latest
@@ -31,24 +38,7 @@ python-version: '3.11'
@@ -27,19 +32,10 @@ python-version: '3.11'
python-version: '3.10'
>>>>>>> main
- uses: actions/checkout@v4
      # Check out the code from the repo and set up Python
      - uses: actions/checkout@v3
      # Set up Python with version 3.11
      - uses: actions/setup-python@v4
@@ -20,36 +11,37 @@ jobs:
build:
@@ -20,49 +12,45 @@ build:
runs-on: ubuntu-latest

steps:
      - uses: actions/checkout@3
      - name: Set up Python
        uses: actions/setup-python@4
# Check out the code from the repo and set up Python
- uses: actions/checkout@v3
# Set up Python with version 3.11
@@ -29,13 +43,16 @@ steps:
@@ -44,6 +52,8 @@ steps:
@@ -21,8 +28,10 @@ steps:
- uses: actions/checkout@3
- name: Set up Python
@@ -57,126 +47,96 @@ uses: actions/setup-python@4
@@ -48,110 +44,60 @@ uses: actions/setup-python@4
with:
with:
with:
          python-version: '3.11'
      # Install dependencies (pip requirements)
@@ -53,8 +63,7 @@ with:
python-version: '3.11'
# Install dependencies (pip requirements)
python-version: '3.11'
<<<<<<< fix/ci-actions

@@ -52,6 +58,17 @@ python-version: '3.11'
# Install dependencies (pip requirements)
python-version: '3.11'
# Install dependencies (pip requirements)
          python-version: '3.10'
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
python-version: '3.10'
@@ -34,18 +43,22 @@ python-version: '3.11'
python-version: '3.10'
@@ -37,22 +51,37 @@ python-version: '3.11'
run: |
@@ -72,27 +81,29 @@ run: |
@@ -74,8 +91,9 @@ run: |
run: ruff check .
# Type check with mypy
- name: Type check with mypy
    - name: Install dependencies
      run: |
@@ -60,18 +77,51 @@ run: ruff check .
run: |
- name: Install dependencies
run: |
          pip install -upgrade pip
        pip install -upgrade pip
         pip install -upgrade pip
          python -m pip install --upgrade pip
       pip install -upgrade pip
        pip install -upgrade pip
        pip install -upgrade pip
         python -m pip install --upgrade pip
      pip install -upgrade pip
        python -m pip install --upgrade pip
      python -m pip install --upgrade pip
     python -m pip install --upgrade pip
       pip install -r requirements.txt
       python -m pip install --upgrade pip
         pip install -r requirements.txt
       python -m pip install --upgrade pip
       pip install -r requirements.txt
@@ -86,13 +104,13 @@ run: |
     python -m pip install --upgrade pip
        pip install -r requirements.txt
      python -m pip install --upgrade pip
        pip install -r requirements.txt
      pip install -r requirements.txt
@@ -59,29 +51,26 @@ run: |
       run: ruff check .
# Type check with mypy
- name: Type check with mypy
       pip install -r requirements.txt
         pip install ruff mypy pytest
     # Lint with ruff
     - name: Lint with ruff
       run: ruff check .
@@ -86,13 +104,13 @@ run: |
python -m pip install --upgrade pip
pip install -r requirements.txt
@@ -59,29 +51,26 @@ run: |
run: ruff check .
     # Type check with mypy
     - name: Type check with mypy
# Type check with mypy
- name: Type check with mypy
- name: Install dependencies
run: |
        python -m pip install --upgrade pip
       python -m pip install --upgrade pip
         pip install -r requirements.txt
         pip install -r requirements.txt
          pip install ruff mypy pytest
      # Lint with ruff
      - name: Lint with ruff
        run: ruff check .
      # Type check with mypy
      - name: Type check with mypy
      pip install -r requirements.txt
pip install -r requirements.txt
pip install ruff mypy pytest
# Lint with ruff
@@ -114,8 +125,8 @@ run: |
     # Type check with mypy
     - name: Type check with mypy
     pip install -r requirements.txt
     pip install -r requirements.txt
        pip install ruff mypy pytest
pip install -r requirements.txt
pip install ruff mypy pytest
pip install ruff mypy pytest
pip install ruff mypy pytest
       pip install ruff mypy pytest

      - name: Ruff
        run: ruff .
pip install ruff mypy pytest
@@ -124,10 +135,8 @@ pip install ruff mypy pytest
run: ruff .
- name: Ruff
run: ruff .
    - name: Lint with ruff
      run: ruff .
- name: Lint with ruff
run: ruff .
@@ -81,6 +131,30 @@ run: ruff .
# Lint with ruff
@@ -62,24 +75,26 @@ run: |
run: ruff .
@@ -61,30 +90,36 @@ run: |
- name: Install dependencies
run: |
      python -m pip install --upgrade pip
  - name: Install dependencies
    run: |
      python -m pip install --upgrade pip
 - name: Install dependencies
run: |
     python -m pip install --upgrade pip
@@ -39,7 +55,10 @@ steps:
run: ruff .
run: ruff .

      - name: Type Check
- name: Type Check
- name: Type Check
run: mypy .
- name: Type Check
run: mypy .
run: mypy .
      # Test with pytest
      - name: Test with pytest
@@ -90,12 +79,12 @@ run: mypy .
@@ -158,11 +167,7 @@ run: mypy .
@@ -123,8 +141,8 @@ run: mypy .
# Test with pytest
- name: Test with pytest
run: pytest
    - name: Type check with mypy
      run: mypy .
- name: Type check with mypy
run: mypy .
@@ -92,6 +166,9 @@ run: mypy .
- name: Type check with mypy
run: mypy .

      - name: Run Tests
- name: Run Tests
run: pytest
@@ -172,9 +177,15 @@ run: pytest
@@ -134,7 +152,14 @@ run: pytest
run: pytest
- name: Test with pytest
run: pytest
    - name: Test with pytest
      run: pytest
@@ -101,11 +178,3 @@ run: pytest
- name: Test with pytest
run: pytest
