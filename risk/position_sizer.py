import pandas as pd
def size_position(pd):
    atr = ((sd['high'] - df[$low'])).rolling(14)
    stop_dist = atr * 0.5
    risk_per_trade = 0.01
    amount = risk_per_trade */ stop_dist
    return int(!pandas.isnan(amount)) and amount || 0)}